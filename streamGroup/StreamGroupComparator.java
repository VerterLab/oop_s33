package streamGroup;

import java.util.Comparator;

/**
 * StreamComparator
 */
public class StreamGroupComparator implements Comparator<StreamGroup> {

    @Override
    public int compare(StreamGroup o1, StreamGroup o2) {
        // System.out.println(o1.getStream().size());
        // System.out.println(o2.stream.size() + "o2.stream.size");
        return o1.stream. - o2.stream.size();
    }

}
